from utils import max_element

numbers = [3, 1, 4, 1, 5, 9]

# by inbuilt max function
max_value_builtin = max(numbers)

max_value = max_element.maxi(numbers)
print(f"The maximum value in the list is: {max_value}")
print(f"The maximum value in the list is: {max_value}")